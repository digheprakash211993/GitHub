<apex:page controller="pract21">
    <apex:form >
    <apex:pageBlock >
        <apex:pageBlockTable value="{!demo}" var="acc">
        <apex:column value="{!acc.name}"/>
        <apex:column value="{!acc.id}"/>
        </apex:pageBlockTable>
        
        </apex:pageBlock>
    </apex:form>
</apex:page>