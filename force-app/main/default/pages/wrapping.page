<apex:page controller="wrapping">
    <apex:form >
    <apex:pageBlock >
        <apex:pageBlockTable value="{!iobj}" var="acc">
        <apex:column headerValue="First name" value="{!acc.fname}"/>
        <apex:column headerValue="last name" value="{!acc.lastname}"/>
            <apex:column headerValue="Country name" value="{!acc.Cname.name}"/>
            <apex:column headerValue="Hotel name" value="{!acc.Hname.name}"/>
        
        </apex:pageBlockTable>
        </apex:pageBlock>
    </apex:form>
</apex:page>